# ============================================================
# 林卡电表统计 — Docker Compose
# ============================================================
# 启动:  docker compose up -d
# 停止:  docker compose down
# 日志:  docker compose logs -f
# 重启:  docker compose restart
# ============================================================

services:
  linclub:
    build: .
    container_name: linclub
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      # 数据持久化（重要！）
      - linclub-data:/data
    environment:
      # 端口配置（默认 8765）
      - LINCLUB_PORT=8765
      # 数据目录（默认 /data）
      - LINCLUB_DATA_DIR=/data
      # 绑定地址（默认 0.0.0.0，容器内用 0.0.0.0）
      - LINCLUB_BIND=0.0.0.0
      # 初始管理员密码（请修改！默认 admin123）
      - LINCLUB_INITIAL_PASS=admin123
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8765/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  linclub-data:
    driver: local
